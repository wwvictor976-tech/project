import { ResourcePage } from '@/components/shared/ResourcePage';
export default function Planos() { return <ResourcePage title="Planos" description="Gestão de planos comerciais e limites de assinatura." status="empty" emptyTitle="Nenhum plano carregado" emptyDescription="Planos reais serão exibidos pela camada de serviços quando o backend estiver implementado." />; }
